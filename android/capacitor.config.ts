import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
appId: "com.ebanking.app",
appName: "ebanking",
webDir: "www",
server: {
androidScheme: "https",
}
};

export default config;
